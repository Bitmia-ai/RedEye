---
description: "Start or resume the autonomous dev loop"
---

Invoke the `redeye:start` skill. The skill is responsible for running `start-loop.sh`, validating loop infrastructure, and entering the loop — do not invoke the script yourself.
